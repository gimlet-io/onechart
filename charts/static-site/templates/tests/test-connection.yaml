{{- $prem := false }}
{{- if $prem }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "static-site.fullname" . }}-test-connection"
  labels:
    {{- include "static-site.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "static-site.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
{{- end }}