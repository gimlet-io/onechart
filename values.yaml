ingress:
  host: chart-example.local
  ingressClassName: nginx

image:
  repository: nginx
  tag: latest

vars:
  MY_VAR: "value"

volumes:
  - name: testing
    path: /deleteme
    storageClass: test
    size: 1Gi
    pvcAnnotations:
      rancher.io/local-path/volume-type: local